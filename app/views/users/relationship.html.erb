<h1>关系</h1>

<h3>好友</h3>
<ul class="users">
  <% @friend.each do |user| %>
    <li>
      <%= link_to user.name, user %> 注册于 <%= user.created_at.to_date %>
    </li>
  <% end %>
</ul>

<h3>关注</h3>
<ul class="users">
  <% @following.each do |user| %>
    <li>
      <%= link_to user.name, user %> 注册于 <%= user.created_at.to_date %>
    </li>
  <% end %>
</ul>

<h3>粉丝</h3>
<ul class="users">
  <% @followers.each do |user| %>
    <li>
      <%= link_to user.name, user %> 注册于 <%= user.created_at.to_date %>
    </li>
  <% end %>
</ul>
