<table>
  <tr>
    <th>活动标题</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @activities.each do |activity| %>
  <tr>
    <td><%= link_to activity,activity %></td>
    <td><%= link_to t('labels.show'), activity %></td>
    <td><%= link_to t('labels.edit'), edit_activity_path(activity) %></td>
    <td><%= link_to t('labels.destroy'), activity, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

